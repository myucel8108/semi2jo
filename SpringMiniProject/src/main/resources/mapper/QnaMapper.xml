<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="bit.data.dao.QnaDao">
	<select id="getMaxNum" resultType="int">
		select ifnull(max(num),0) from qna
	</select>
	<select id="getTotalCount" parameterType="Map" resultType="int">
		select count(*) from qna
		<if test="searchcolumn!=null and searchword!=null">
			where ${searchcolumn} like concat('%',#{searchword},'%')
		</if>
	</select>
	<select id="getPagingList" parameterType="Map" resultType="QnaDto">
		select * from qna
		<if test="searchcolumn!=null and searchword!=null">
			where ${searchcolumn} like concat('%',#{searchword},'%')
		</if>
		 order by regroup desc,restep asc limit #{startnum},#{perpage}
	</select>
	<insert id="insertBoard" parameterType="QnaDto">
		insert into qna values (null,#{id},#{name},#{subject},#{photo},#{content},0,0,#{regroup},#{restep},#{relevel},now())
	</insert>
	<update id="updateRestep" parameterType="Map">
		update qna set restep = restep+1 where regroup = #{regroup} and restep = #{restep}
	</update>

	<select id="selectByNum" parameterType="int" resultType="QnaDto">
		select * from qna where qnanum = #{qnanum}
	</select>
	<update id="updateBoard" parameterType="QnaDto">
		update qna set subject = #{subject},content=#{content}
		<if test="photo!=null">
			,photo = #{photo}
		</if>
		 where num = #{qnanum}
	</update>
	<delete id="deleteBoard" parameterType="int">
		delete from qna where num = #{num}
	</delete>


</mapper>